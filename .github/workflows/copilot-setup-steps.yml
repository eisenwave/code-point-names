name: Copilot Setup Steps

on:
  workflow_dispatch:

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y \
            g++-14 cmake ninja-build clang-format-20

      - name: Create build directory
        run: cmake -E make_directory build

      - name: Download UnicodeData.txt
        run: |
          curl -fL "https://unicode.org/Public/17.0.0/ucd/UnicodeData.txt" \
               -o build/UnicodeData.txt
      - name: Download NameAliases.txt
        run: |
          curl -fL "https://unicode.org/Public/17.0.0/ucd/NameAliases.txt" \
               -o build/NameAliases.txt
